<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.icaro.filme">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:name="applicaton.FilmeApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity
            android:name="activity.MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
            <meta-data
                android:name="android.app.default_searchable"
                android:value="activity.SearchActivity" />
        </activity>
        <activity
            android:name="activity.FilmeActivity"
            android:theme="@style/AppTheme.NoActionBar.NavDrawer"
            android:parentActivityName="activity.MainActivity">
            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
            <meta-data
                android:name="android.app.default_searchable"
                android:value="activity.SearchActivity" />
        </activity>
        <activity
            android:name="activity.SearchActivity"
            android:theme="@style/AppTheme.NoActionBar.NavDrawer"
            android:parentActivityName="activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>

            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
        </activity>
        <activity android:name="activity.TreilerActivity" />
        <activity
            android:name="activity.PosterActivity"
            android:theme="@style/Theme.AppCompat.Light.Dialog.MinWidth.NoActionBar" />
        <activity android:name="activity.ReviewsActivity" />
        <activity
            android:name="activity.ElencoActivity"
            android:theme="@style/AppTheme.NoActionBar.NavDrawer"
            android:parentActivityName="activity.FilmeActivity">
            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
            <meta-data
                android:name="android.app.default_searchable"
                android:value="activity.SearchActivity" />
        </activity>
        <activity
            android:name="activity.CrewsActivity"
            android:theme="@style/AppTheme.NoActionBar.NavDrawer"
            android:parentActivityName="activity.FilmeActivity">
            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
            <meta-data
                android:name="android.app.default_searchable"
                android:value="activity.SearchActivity" />
        </activity>
        <activity android:name="activity.PosterGridActivity" />
        <activity
            android:name="activity.FavotireActivity"
            android:parentActivityName="activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>

            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
        </activity>
        <activity android:name="activity.SiteTMDB" />


        <activity android:name="activity.WatchListActivity">
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>

            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />


        </activity>
        <activity android:name="activity.RatedActivity">
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>

            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
        </activity>

        <activity android:name="activity.ProdutoraActivity"
            android:theme="@style/AppTheme.NoActionBar.NavDrawer"/>
    </application>

</manifest>